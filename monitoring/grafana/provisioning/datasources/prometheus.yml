# Grafana datasource provisioning (v1.1 observability).
# Points Grafana at the Prometheus service on the internal Docker network.
apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
