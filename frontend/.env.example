# Base URL for the backend REST API.
# In dev you can leave this unset and rely on the Vite proxy (/api -> :8080),
# or set it explicitly to the backend origin + /api.
VITE_API_BASE_URL=http://localhost:8080/api
