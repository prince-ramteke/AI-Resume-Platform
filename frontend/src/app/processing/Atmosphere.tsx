/**
 * L1 — radial gradient wash behind the document theatre. Theme-aware via
 * CSS custom properties. Low opacity ambient glow using the accent cyan.
 */
export function Atmosphere() {
  return <div className="theatre-atmosphere" aria-hidden="true" />;
}
